#include "Oferecimento.h"

Oferecimento::Oferecimento( void )
{

}

Oferecimento::~Oferecimento( void )
{

}

/*
void Oferecimento::escreve_arvore( ItemAtendimento & elem )
{
   elem.id( id );
   elem.ofertaCursoCampiId( oferta_curso_campi_id );
   elem.disciplinaId( disciplina_id );
   elem.turma( turma );
   elem.quatidadeAlunos( quantidade_alunos );
   elem.salaId( sala_id );
   elem.diaSemana( dia_semana );

   if ( alocacao_tatico )
   {
      alocacao_tatico->escreve_arvore(elem.alocacaoTatico().get() );
   }
   else if( alocacao_operacional )
   {
      alocacao_operacional->escreve_arvore( elem.alocacaoOperacional().get() );
   }
}
*/

